import 'package:flutter/material.dart';
import 'package:seed_yap/src/block_widget.dart';

class BlockPage extends StatelessWidget {
  const BlockPage({Key? key}) : super(key: key);

  @override
  @override
  Widget build(BuildContext context) {
    return (BlockWidget());
  }
}
